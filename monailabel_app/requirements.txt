# MONAI Label app environment, pinned to what was run (Linux, Python 3.12)
# install torch 2.11.0 for your CUDA first, then:
monai[nibabel,itk]==1.5.2
monailabel==0.8.5
# monailabel 0.8.5 metadata allows newer versions of these, but they break its imports
pydicom==2.4.4
pydicom-seg==0.4.1
dicomweb-client==0.59.1
requests
